import { ReactNode } from 'react';
export type Column<T>={label:string;render:(item:T)=>ReactNode};
export default function DataTable<T extends {id:number}>({items,columns}:{items:T[];columns:Column<T>[]}){return <div className="panel overflow-x-auto"><table className="w-full text-left text-sm"><thead className="border-b border-slate-800 text-xs uppercase tracking-wide text-slate-500"><tr>{columns.map(c=><th key={c.label} className="px-5 py-4">{c.label}</th>)}</tr></thead><tbody className="divide-y divide-slate-800">{items.map(item=><tr key={item.id} className="hover:bg-slate-800/30">{columns.map(c=><td key={c.label} className="px-5 py-4">{c.render(item)}</td>)}</tr>)}</tbody></table>{items.length===0&&<div className="p-10 text-center text-slate-500">No hay registros todavía.</div>}</div>}
