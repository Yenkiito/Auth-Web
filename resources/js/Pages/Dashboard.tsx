import AppShell from '@/Components/AppShell';
import { Head } from '@inertiajs/react';
import { Activity, Cpu, FolderKanban, KeyRound, ShieldCheck, Users, UserRound } from 'lucide-react';
type Props={stats:Record<string,number>;activity:Array<{id:number;action:string;actor:string;created_at:string}>};
export default function Dashboard({stats,activity}:Props){const cards=[['Proyectos','projects',FolderKanban],['Socios','partners',Users],['Clientes','clients',UserRound],['Licencias','licenses',KeyRound],['Activas','active',ShieldCheck],['Expiradas','expired',Activity],['Dispositivos','devices',Cpu]] as const;return <AppShell title="Dashboard"><Head title="Dashboard"/><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{cards.map(([label,key,Icon])=><div className="panel p-5" key={key}><div className="mb-5 flex items-center justify-between text-slate-400"><span className="text-sm">{label}</span><Icon size={18}/></div><strong className="text-3xl">{stats[key]??0}</strong></div>)}</div><div className="panel mt-6 p-5"><h2 className="mb-4 font-semibold">Actividad reciente</h2><div className="space-y-3">{activity.map(x=><div key={x.id} className="flex justify-between border-b border-slate-800 pb-3 text-sm"><span><b className="text-cyan-300">{x.actor}</b> · {x.action}</span><span className="text-slate-500">{new Date(x.created_at).toLocaleString()}</span></div>)}{activity.length===0&&<p className="text-sm text-slate-500">Sin actividad registrada.</p>}</div></div></AppShell>}
