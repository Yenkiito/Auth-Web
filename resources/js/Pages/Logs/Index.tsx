import AppShell from '@/Components/AppShell';import DataTable from '@/Components/DataTable';import {Head} from '@inertiajs/react';type Log={id:number;actor:string;action:string;ip?:string;metadata?:Record<string,unknown>;created_at:string};export default function Index({items}:{items:{data:Log[]}}){return <AppShell title="Actividad"><Head title="Actividad"/><DataTable items={items.data} columns={[{label:'Actor',render:l=><b>{l.actor}</b>},{label:'Acción',render:l=><span className="text-cyan-300">{l.action}</span>},{label:'IP',render:l=>l.ip??'—'},{label:'Detalles',render:l=><code className="text-xs text-slate-500">{l.metadata?JSON.stringify(l.metadata):'—'}</code>},{label:'Fecha',render:l=>new Date(l.created_at).toLocaleString()}]}/></AppShell>}
