import AppShell from '@/Components/AppShell';import {Head,Link} from '@inertiajs/react';export default function Settings(){return <AppShell title="Configuración"><Head title="Configuración"/><div className="panel max-w-2xl p-6"><h2 className="mb-2 text-lg font-semibold">Seguridad de la cuenta</h2><p className="mb-5 text-sm text-slate-400">Administra tu perfil y contraseña. Las credenciales y secretos del servidor nunca se exponen al frontend.</p><Link href={route('profile.edit')} className="btn-primary">Editar perfil</Link></div></AppShell>}
